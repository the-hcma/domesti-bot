[project]
name = "domesti-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gotailwind[cli]",
    "icecream",
    "prompt_toolkit>=3.0.43",
    "pyproj",
    "pyright",
    "pytest",
    "pytest-asyncio",
    "python-kasa",
    "soco>=0.30",
]

[tool.uv]
package = false
